const { makeStyles } = require("@mui/styles");

export const useStyles = makeStyles((theme) => ({
  root: {
    padding: "10px",
    marginBottom: "1rem",
    borderRadius: "10px",
    border: `1px solid green`,
    minHeight: "40px",
  },
}));
